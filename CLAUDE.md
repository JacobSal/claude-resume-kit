@AGENTS.md

---

## Claude Code Specific

The cross-agent source of truth is `AGENTS.md` (imported above): operating modes, skill
location, File Map, canonical rules, LaTeX notation, and state-file conventions all live
there. Edit shared facts THERE, not here. Keep only Claude-Code-specific notes and the
standalone-mode state tables below.

> **Plugin mode:** this `CLAUDE.md` is NOT auto-loaded; skills read `kit_state.md` under
> `RESUME_KIT_ROOT` instead. The tables below are the **standalone-mode** state location.

---

## Active Sessions

_Standalone-mode state. Update when starting/finishing a JD. (Plugin mode uses `kit_state.md`.)_

| Session | Status | Next Command |
|---------|--------|-------------|
| (none active) | — | — |

---

## KB Corrections Log

_Standalone-mode state. See `config.md` for user-specific corrections. Add verified errors
here as you find them. (Plugin mode uses `kit_state.md`.)_
